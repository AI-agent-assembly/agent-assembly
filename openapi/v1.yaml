openapi: 3.1.0

info:
  title: Agent Assembly API
  version: 0.0.1
  description: |
    REST API for the Agent Assembly governance gateway.

    This spec is auto-generated by CI from `aa-api` route annotations via
    `utoipa`. The file is checked in as a contract artefact — CI fails if
    the generated spec drifts from this committed version.
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  contact:
    name: Agent Assembly Contributors
    url: https://github.com/AI-agent-assembly/agent-assembly

servers:
  - url: http://localhost:8080/v1
    description: Local development gateway

paths: {}
